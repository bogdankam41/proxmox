# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Secrets — real tfvars are never committed, examples are
*.tfvars
!*.tfvars.example

# Ansible
*.retry
# Inventories are generated by Terraform (modules/k3s_cluster)
ansible/inventory/*.ini

# OS files
.DS_Store

# Note: .terraform.lock.hcl is intentionally committed (pins provider versions).

# Claude folder
.claude/