# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Secrets — real tfvars are never committed, examples are
*.tfvars
!*.tfvars.example

# Ansible
*.retry

# OS files
.DS_Store

# Note: .terraform.lock.hcl is intentionally committed (pins provider versions).

# Claude folder
.claude/